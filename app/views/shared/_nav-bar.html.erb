<nav class="navbar bg-primary navbar-dark">

  <div class="d-flex justify-content-between mx-2" style='width: 100%;'>

    <%# Left side icon on the nav bar %>
    <div class=''>
      <% if current_page?(new_user_session_path) || current_page?(new_user_registration_path) || @new_transaction_page || @new_group_page || @edit_group_page %>
        <%= link_to "◁", :back, class:'link-light', style:'' %>
      <% elsif @group_show_page %>
        <%= link_to "◁", :groups, class:'link-light', style:'' %>
      <% else %>
        <div class="" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </div>
      <% end %>
    </div>

    <%# Word for the categor %>
    <div class="d-flex align-items-md-center">
      <% if @user_page %>
        <h5 class='text-white'>Index</h5>

      <% elsif @group_page %>
        <h5 class='text-white'>TRANSACTIONS</h5>

      <% elsif @group_show_page %>
        <h5 class='text-white'>Details</h5>

      <% elsif @movement_page %>
        <h3 class='text-white'>Movements</h3>

      <% elsif @new_transaction_page %>
        <h3 class='text-white'>New transaction</h3>

      <% elsif @new_group_page %> 
        <h3 class='text-white'>New category</h3>
      <% elsif @edit_group_page %> 
        <h3 class='text-white'>Edit category</h3>


      <% else %>
        <% if current_page?(new_user_registration_path) %>
          <h5 class='text-white'>REGISTER</h5>
        <% elsif current_page?(new_user_session_path) %>
          <h5 class='text-white'>LOGIN</h5>
       <% end %>
      <% end %>
    </div>

    <%# Right side icon on the nav bar %>
    <div>
      <% if current_page?(new_user_session_path) %>
        <%= link_to 'Log in', new_user_session_path, class: 'link-light' %>
      <% elsif current_page?(new_user_registration_path) %>
        <%= link_to 'Next', new_user_session_path, class: 'link-light' %>
      <% else %>
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: 'sign_out_btn' %>
      <% end %>
    </div>
  </div>

  <%# Conten hamburguer button menu %>
  <div class="collapse navbar-collapse" id="navbarSupportedContent" style='width: 100%;'>
  
    <% if user_signed_in? %>

      <div>

        <div class= "navbar-nav">
          
          <h4 class="text-white mt-2 mr-2"><%= link_to "◁", :groups, class:'link-light', style:'' %> Welcome <%= current_user.name %></h4>
        </div>

      </div>

    <% else %>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <ul class="d-flex navbar-nav mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Log In", new_user_session_path, class:"nav-link"%>
            </li>
            <li class="nav-item">
              <%= link_to "Sign Up", new_user_registration_path, class:"nav-link"%>
            </li>
        </ul>

      </div>

    <% end %>
  </div>
</nav>